Line One WebRTC SIP Phone 使用说明

1. 解压后双击 LineOnePhone.exe。
2. 程序会在后台启动，并自动打开浏览器页面。
3. 如果重复双击，程序会打开已有页面，不会重复占用端口。
4. 需要完全退出时，请点击页面右上角“退出程序”，程序会断开注册并释放端口。
5. 信令记录仅保存在当前运行期间，程序重启后会清空。
6. 排查问题时，可以在“信令交互”窗口点击“导出”，保存当前信令文本。
